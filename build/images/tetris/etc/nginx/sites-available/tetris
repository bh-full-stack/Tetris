server {
	listen 80;
	listen [::]:80;

	root /var/www/html/public/;

	index index.html;

	server_name tetris;

	location / {
		try_files $uri $uri/ /index.html;
	}

	location ~ /\.ht {
		deny all;
	}
}
